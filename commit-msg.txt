sitemap.xml: refresh for current SPA state

The old sitemap (from Phase 2) was stale:
- Used the old hash format (#/crops) — broken since the browser-router
  migration in Phase 14
- Missing all the routes added in Phase 12 (pets was already there,
  but gears, eggs, seed-packs, mutations were not)
- Missing per-crop detail URLs
- No <lastmod> fields

Generated a fresh public/sitemap.xml with:
- 10 top-level routes (home + 9 category pages) in priority order
- 30 per-crop detail URLs (slug list parsed from src/data/crops.ts)
- <lastmod>2026-06-13</lastmod> on every entry
- <changefreq> and <priority> per page type (home=daily/1.0,
  crops=daily/0.9, calculator/compare=weekly/0.9, etc.)

Total: 40 URLs.

Public/ robots.txt was already correct from Phase 2 (allows all,
points to sitemap). Left as-is.

If a future commit adds a new route or crop, the sitemap needs
to be regenerated. A Vite plugin could automate this from the
data files, but a static file is fine for the current scale (40 URLs).
