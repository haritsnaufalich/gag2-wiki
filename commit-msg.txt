Lock: site deploys to /gag2-wiki/ subpath (D14)

Per the user directive "KEEP IT ON /gag2-wiki", make the
github.io subpath explicit and non-trivial to change.

Verified the current state already matches: Vite base is "./"
(relative — works on any host), SITE_URL constant in the
SEO helper is the github.io subpath, the sitemap uses it,
and the gh-pages deploy is live at that URL.

What changed:
- vite.config.ts: added a 10-line comment above `base: "./"`
  explaining the github.io subpath lock, why relative paths
  are the right choice, and a DO NOT change warning.
- MEMORY.md: added D14 capturing the same intent for future
  sessions.

No code behavior change. The site was already on /gag2-wiki
and continues to be.
